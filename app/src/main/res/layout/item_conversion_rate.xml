<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="data"
            type="com.macv.currencyconverter.model.data.CurrencyRate" />

        <import type="com.macv.currencyconverter.utils.extension.CommonKt"/>

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="?attr/listPreferredItemPaddingStart">

        <TextView
            android:id="@+id/currencyName"
            android:layout_width="0dp"
            app:layout_constraintEnd_toStartOf="@+id/currencyRate"
            android:layout_height="wrap_content"
            android:text="@{ CommonKt.removePrefix(data.currencyName) }"
            android:textAppearance="?attr/textAppearanceListItem"
            android:textStyle="bold"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="Name" />

        <TextView
            android:id="@+id/currencyRate"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text='@{ String.valueOf(CommonKt.getDecimalFormat(data.currencyRate)) }'
            android:textAppearance="?attr/textAppearanceListItemSmall"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="23.00" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>
